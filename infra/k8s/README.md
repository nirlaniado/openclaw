# Kubernetes Placeholder

Design constraints for later:

- stateless app containers
- externalized secrets and config
- DB and auth accessed through env-configured clients
- future Ollama backend isolated behind a service contract
